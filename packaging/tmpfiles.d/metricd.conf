# Protect metricd socket from systemd-tmpfiles cleanup
# The daemon recreates the socket automatically, but this avoids
# the brief window where the socket file is missing.
x /run/user/%U/metricd.sock
